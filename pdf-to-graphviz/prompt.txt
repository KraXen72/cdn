copy the styling (visuals, colors) of this project. similary to how the images are outputted here, i'd like the output to be a list of cards, maaybe of 2x 16:9 (approx) aspect ratios, LHS would be the code in a textarea (scrollable) and rhs the graphviz rendering (use the wasm graphviz thingy) of that particular graph, which, when clicked, would open in some bigger, scrollable thing at the top (only one open at at a ttime), so i can quickly spot any discrepancies.