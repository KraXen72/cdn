#!/bin/bash
# Pre-commit hook: regenerate both listings and include them in the commit
python3 "$(git rev-parse --show-toplevel)/generate-index.py" &&
git add "$(git rev-parse --show-toplevel)/index.html" "$(git rev-parse --show-toplevel)/_artifacts.html"
